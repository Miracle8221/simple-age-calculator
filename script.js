// Creat variables to accept birth year and current year user input
const a = parseInt(prompt("Enter birth year: "));
const b = parseInt(prompt("Enter current year: "));
// create an addition function
// The addition function will return the addition for input a and b
// Invoke addition function to run the calculator